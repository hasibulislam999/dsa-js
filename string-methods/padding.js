/* include desire number of zero or anything before an element */
// only works with string
const str = '5';
// padding at start
const newStr = str.padStart(3, '0');
// padding at end
// const newStr = str.padEnd(3, '0');
console.log(newStr);